import React from "react";

const EditPost = () => {
    const handleClick = () => {};
    return (
        <button className="mod-supp" onClick={handleClick}>
            Modifer
        </button>
    );
};

export default EditPost;
